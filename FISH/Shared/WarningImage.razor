@using Microsoft.AspNetCore.Components.Rendering
@using System.Diagnostics.CodeAnalysis
@inject NavigationManager NavigationManager

<ImageViewer Url="@aUrl" FitMode="BootstrapBlazor.Components.ObjectFitMode.Cover"/>
<ImageViewer Url="@aUrl2" FitMode="BootstrapBlazor.Components.ObjectFitMode.Cover"/>
<ImageViewer Url="@aUrl3" FitMode="BootstrapBlazor.Components.ObjectFitMode.Cover"/>

@code {
    [Parameter] public string aUrl { get; set; }
    [Parameter] public string aUrl2 { get; set; }
    [Parameter] public string aUrl3 { get; set; }

    public enum ObjectFitMode
    {
        Fill,
        Contain,
        Cover,
        None,
        ScaleDown
    }


    [Parameter] public ObjectFitMode FitMode { get; set; } = ObjectFitMode.Cover;

    // protected override void BuildRenderTree(RenderTreeBuilder builder)
    // {
    //     builder.OpenElement(0, "img");
    //     builder.AddAttribute(1, "src", "/asset/img/IMG_0170.jpg");
    //     builder.AddAttribute(2, "style", $"object-fit: {FitMode.ToString().ToLower()};");
    //     builder.CloseElement();
    // }


    protected override async Task OnInitializedAsync()
    {
        aUrl = $"./images/warnning/IMG_0170.JPG";
        aUrl2 = $"./images/warnning/IMG_0171.JPG";
        aUrl3 = $"./images/warnning/IMG_0172.JPG";
    }

}